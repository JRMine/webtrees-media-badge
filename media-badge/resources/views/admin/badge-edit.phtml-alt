<?php

declare(strict_types=1);

/**
 * @var array<string,mixed> $rule
 * @var string $title
 * @var \Vendor\Webtrees\Module\MediaBadge\MediaBadgeModule $module
 */

use function route;
?>

<div class="mbg-admin-page">
    <h2><?= e($title) ?></h2>

    <form method="post" action="<?= e(route('module', ['module' => $module->name(), 'action' => 'BadgeEdit'])) ?>">
        <?= csrf_field() ?>

        <input type="hidden" name="id" value="<?= e((string) $rule['id']) ?>">

        <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" id="enabled" name="enabled" value="1" <?= $rule['enabled'] ? 'checked' : '' ?>>
            <label class="form-check-label" for="enabled">Regel aktiv</label>
        </div>

        <div class="row g-3">
            <div class="col-md-6">
                <label class="form-label" for="key">Key</label>
                <input class="form-control" id="key" name="key" type="text" value="<?= e((string) $rule['key']) ?>" placeholder="MEDIA LICENCE">
            </div>

            <div class="col-md-6">
                <label class="form-label" for="position">Position</label>
                <select class="form-select" id="position" name="position">
                    <option value="before-title" <?= $rule['position'] === 'before-title' ? 'selected' : '' ?>>vor dem Titel</option>
                    <option value="after-title" <?= $rule['position'] === 'after-title' ? 'selected' : '' ?>>nach dem Titel</option>
                </select>
            </div>

            <div class="col-md-4">
                <label class="form-label" for="match_type">Match-Typ</label>
                <select class="form-select" id="match_type" name="match_type">
                    <option value="exact" <?= $rule['match_type'] === 'exact' ? 'selected' : '' ?>>exact</option>
                    <option value="contains" <?= $rule['match_type'] === 'contains' ? 'selected' : '' ?>>contains</option>
                    <option value="regex" <?= $rule['match_type'] === 'regex' ? 'selected' : '' ?>>regex</option>
                </select>
            </div>

            <div class="col-md-8">
                <label class="form-label" for="match_value">Match-Wert</label>
                <input class="form-control" id="match_value" name="match_value" type="text" value="<?= e((string) $rule['match_value']) ?>" placeholder="CC BY 4.0">
            </div>

            <div class="col-md-6">
                <label class="form-label" for="label">Badge-Text</label>
                <input class="form-control" id="label" name="label" type="text" value="<?= e((string) $rule['label']) ?>" placeholder="CC BY 4.0">
            </div>

            <div class="col-md-6">
                <label class="form-label" for="class">CSS-Klasse</label>
                <input class="form-control" id="class" name="class" type="text" value="<?= e((string) $rule['class']) ?>" placeholder="mbg-badge mbg-badge--ccby">
            </div>

            <div class="col-md-8">
                <label class="form-label" for="title">Tooltip</label>
                <input class="form-control" id="title" name="title" type="text" value="<?= e((string) $rule['title']) ?>" placeholder="Creative Commons Attribution 4.0">
            </div>

            <div class="col-md-4">
                <label class="form-label" for="sort_order">Reihenfolge</label>
                <input class="form-control" id="sort_order" name="sort_order" type="number" value="<?= e((string) $rule['sort_order']) ?>">
            </div>
        </div>

        <div class="d-flex gap-2 mt-4">
            <button type="submit" class="btn btn-primary">Speichern</button>

            <a class="btn btn-outline-secondary" href="<?= e(route('module', ['module' => $module->name(), 'action' => 'Badges'])) ?>">
                Abbrechen
            </a>
        </div>
    </form>
</div>
